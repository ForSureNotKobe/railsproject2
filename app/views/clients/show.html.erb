<% provide(:title, @client.name) %>
<div class="row">
  <aside class="col-md-4">
    <% if @company = Company.find(params[:company_id]).clients.any? %>
      <ol class="clients">
        <%= render @clients %>
      </ol>
    <% end %>
  </aside>
<div>
